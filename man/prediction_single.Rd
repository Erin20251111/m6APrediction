% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/main.R
\name{prediction_single}
\alias{prediction_single}
\title{Predict m6A status for a single observation}
\usage{
prediction_single(
  ml_fit,
  gc_content,
  RNA_type,
  RNA_region,
  exon_length,
  distance_to_junction,
  evolutionary_conservation,
  DNA_5mer,
  positive_threshold = 0.5
)
}
\arguments{
\item{ml_fit}{A fitted classification model that supports
\code{predict(..., type = "prob")} and returns a column named \code{"Positive"}.}

\item{gc_content}{Numeric GC content feature.}

\item{RNA_type}{Character or factor; one of \code{c("mRNA","lincRNA","lncRNA","pseudogene")}.}

\item{RNA_region}{Character or factor; one of \code{c("CDS","intron","3'UTR","5'UTR")}.}

\item{exon_length}{Numeric exon length feature.}

\item{distance_to_junction}{Numeric distance-to-junction feature.}

\item{evolutionary_conservation}{Numeric evolutionary conservation feature.}

\item{DNA_5mer}{Character DNA 5-mer sequence (e.g., \code{"ATCGA"}).}

\item{positive_threshold}{Numeric threshold (0-1) for Positive vs Negative; default 0.5.}
}
\value{
A named vector of length 2:
\code{c(predicted_m6A_prob = <numeric>, predicted_m6A_status = <character>)}.
}
\description{
Convenience wrapper around \code{prediction_multiple()} for one set of
features, returning a named vector with probability and status.
}
\examples{
rf_fit <- readRDS(system.file("extdata", "rf_fit.rds", package = "m6APrediction"))
df <- read.csv(system.file("extdata", "m6A_input_example.csv", package = "m6APrediction"))
r1 <- df[1, ]
prediction_single(
  ml_fit = rf_fit,
  gc_content = r1$gc_content,
  RNA_type = as.character(r1$RNA_type),
  RNA_region = as.character(r1$RNA_region),
  exon_length = r1$exon_length,
  distance_to_junction = r1$distance_to_junction,
  evolutionary_conservation = r1$evolutionary_conservation,
  DNA_5mer = r1$DNA_5mer,
  positive_threshold = 0.5
)

}
